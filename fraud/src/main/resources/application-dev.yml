spring:
  datasource:
    username: grobikon
    password: postgres
    url: jdbc:postgresql://localhost:5433/fraud

  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update